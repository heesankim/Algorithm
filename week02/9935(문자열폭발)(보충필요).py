# 문자열 폭발
import sys
input = sys.stdin.readline
# 문자열이 폭발 > 그문자 사라짐 , 남은 문자열은 다시 합쳐짐
# 폭발하고 나서 이어붙인 문자열에도 폭발문자 모양이있으며 다시 폭파시킨다.
# 폭발문자가 없어질 때 까지 반복된다.
# 남아있는 문자가 없으면 "FRULA" 출력

# def gra(array):
#     if len(array) == 0:
#         print("FRULA")


# 풀지 못했습니다.